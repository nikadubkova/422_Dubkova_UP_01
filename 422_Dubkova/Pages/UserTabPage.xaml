<Page x:Class="_422_Dubkova.Pages.UserTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_422_Dubkova.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Таблица пользователей">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Таблица пользователей -->
        <DataGrid Name="DataGridUsers" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True"
              CanUserSortColumns="False" CanUserReorderColumns="False" CanUserResizeColumns="True"
              SelectionMode="Single" SelectionUnit="FullRow"
              HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="50"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}" Width="70"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="80"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FIO}" Width="200"/>
                <DataGridTextColumn Header="Фото" Binding="{Binding Photo}" Width="50"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Click="EditButton_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки Добавить / Удалить -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button Name="ButtonAdd" Content="Добавить" Width="90" Height="30" Margin="10,0" Click="ButtonAdd_Click"/>
            <Button Name="ButtonDel" Content="Удалить" Width="90" Height="30" Margin="10,0" Click="ButtonDel_Click"/>
        </StackPanel>
    </Grid>

</Page>
