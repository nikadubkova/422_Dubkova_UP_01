<Page x:Class="_422_Dubkova.Pages.PaymentTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_422_Dubkova.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Таблица платежей">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Name="DataGridPayments" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserSortColumns="False" CanUserReorderColumns="False" CanUserResizeColumns="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=d}" Width="70"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding User.FIO}" Width="120"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="100"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Num}" Width="100"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category.Name}" Width="120"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Click="EditButton_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button Name="ButtonAdd" Content="Добавить" Width="90" Height="30" Margin="10,0" Click="ButtonAdd_Click"/>
            <Button Name="ButtonDel" Content="Удалить" Width="90" Height="30" Margin="10,0" Click="ButtonDel_Click"/>
        </StackPanel>
    </Grid>
</Page>